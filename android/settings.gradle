// This file is automatically generated by Expo.
// It contains the configuration for autolinking Expo modules.

rootProject.name = 'garuba-igrf'

apply from: new File(["node", "--print", "require.resolve('expo/package.json')"].execute(null, rootDir).text.trim(), "../scripts/autolinking.gradle")
useExpoModules()

apply from: new File(["node", "--print", "require.resolve('@react-native/gradle-plugin/package.json')"].execute(null, rootDir).text.trim(), "../react-native.gradle")

// Include all the native modules
apply from: new File(["node", "--print", "require.resolve('react-native/package.json')"].execute(null, rootDir).text.trim(), "../packages/react-native/react.gradle")

// Include the app project
include ':app'